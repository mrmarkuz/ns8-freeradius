# RADIUS clients added by hand. Clients created from the module settings are
# kept elsewhere and are not touched by this file.
#
# The example is commented out on purpose: a client with a well-known secret
# lets anybody in that network ask the server to check passwords.
#
#client my-switch {
#	ipaddr = 192.168.1.2
#	secret = use-a-long-random-string
#}
