#
# FreeRADIUS state/backup include patterns for Restic
# Syntax reference: https://pkg.go.dev/path/filepath#Glob
# Restic --files-from: https://restic.readthedocs.io/en/stable/040_backup.html#including-files
# List here what you want to save during backup : volumes or file Path

# hand-edited FreeRADIUS configuration (authorize, clients.conf, mschap, ldap)
state/config
# RADIUS clients set in the module settings, with their shared secrets
state/clients.json
# server certificate and key: a restored server keeps the certificate the
# clients already trust until Traefik provides a new one
state/certs
